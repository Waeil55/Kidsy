import{Bell,Volume2,Accessibility,Download,RefreshCw,Info}from'lucide-react';import{Card}from'../components/Card';import{useApp}from'../store/AppContext';
export function Settings(){const[voice,setVoice]=useState(true);const[notify,setNotify]=useState(true);const[large,setLarge]=useState(false);const{online}=useApp();const install=()=>{alert('PWA install is available from your browser menu when supported.')};return <div className={`page ${large?'largeText':''}`}><div className="pageHead"><div><p className="eyebrow">Control center</p><h1>Settings</h1><p>Customize the learning experience.</p></div></div><div className="settingsGrid"><Card><div className="cardTitle"><Volume2/><h2>Learning experience</h2></div><Toggle label="Read lessons aloud" value={voice} set={setVoice}/><Toggle label="Daily reminders" value={notify} set={setNotify}/><Toggle label="Larger text" value={large} set={setLarge}/></Card><Card><div className="cardTitle"><Accessibility/><h2>Accessibility</h2></div><p className="muted">Keyboard-friendly controls, scalable text, clear focus states and child-friendly contrast are built into the interface.</p><button className="secondary full" onClick={()=>setLarge(v=>!v)}>{large?'Use standard text':'Increase text size'}</button></Card><Card><div className="cardTitle"><Download/><h2>App & offline</h2></div><p className="muted">Status: <b>{online?'Online':'Offline'}</b>. Lessons and progress are stored locally for offline learning.</p><button className="secondary full" onClick={install}><Download/> Install PWA</button><button className="secondary full" onClick={()=>location.reload()}><RefreshCw/> Refresh app</button></Card><Card><div className="cardTitle"><Info/><h2>KidVenture Enterprise</h2></div><p className="muted">Version 2.0 · K–6 learning platform foundation.</p><div className="security">Enterprise-ready boundaries for authentication, content management, analytics, district administration and secure APIs are documented in <b>docs/ENTERPRISE.md</b>.</div></Card></div></div>}
function Toggle({label,value,set}:{label:string;value:boolean;set:(v:boolean)=>void}){return <label className="toggle"><span>{label}</span><button aria-label={label} className={value?'on':''} onClick={()=>set(!value)}><i/></button></label>}
