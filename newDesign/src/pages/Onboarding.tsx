import{useState}from'react';import{Sparkles}from'lucide-react';import{useApp}from'../store/AppContext';import{grades}from'../data/curriculum';export function Onboarding({done}:{done:()=>void}){const{addChild}=useApp();const[name,setName]=useState('');const[grade,setGrade]=useState<any>('K');const[avatar,setAvatar]=useState('🦕');return <div className="onboard"><div className="onboardArt">🚀</div><p className="eyebrow">KidVenture</p><h1>Create your learner profile</h1><p>Tell us about your little learner so we can personalize the adventure.</p><label>Child's name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Enter name"/></label><div className="sectionTitle"><h2>Choose a grade</h2></div><div className="gradeChoose">{grades.map(g=><button className={grade===g?'selected':''} onClick={()=>setGrade(g)} key={g}>Grade {g}</button>)}</div><div className="sectionTitle"><h2>Choose an avatar</h2></div><div className="avatarChoose">{['🦕','🦊','🐼','🦉','🐨','🐯'].map(a=><button className={avatar===a?'selected':''} onClick={()=>setAvatar(a)} key={a}>{a}</button>)}</div><button className="primary full" disabled={!name.trim()} onClick={()=>{addChild({id:crypto.randomUUID(),name,grade,avatar,xp:0,streak:0,completed:[],minutes:0,dailyGoal:20});done()}}><Sparkles/> Create Profile</button></div>}
