import{Award,Flame,Target,TrendingUp,Clock,CheckCircle2}from'lucide-react';import{Card}from'../components/Card';import{useApp}from'../store/AppContext';import{getLessons,subjects}from'../data/curriculum';
export function Progress(){const{child}=useApp();const all=getLessons(child.grade);const completed=new Set(child.completed);const pct=Math.round(completed.size/Math.max(1,all.length)*100);return <div className="page"><div className="pageHead"><div><p className="eyebrow">Learning analytics</p><h1>{child.name}'s progress</h1><p>Grade {child.grade} · Your learning journey at a glance.</p></div></div><section className="progressHero"><div className="progressRing" style={{'--pct':`${pct}%`} as any}><span>{pct}%</span></div><div><p className="eyebrow">Curriculum mastery</p><h2>Keep going!</h2><p>{completed.size} of {all.length} activities completed.</p></div></section><div className="stats"><Card><TrendingUp/><b>{child.xp}</b><span>total XP</span></Card><Card><Flame/><b>{child.streak}</b><span>current streak</span></Card><Card><Clock/><b>{child.minutes}</b><span>minutes learned</span></Card></div><Card><div className="cardTitle"><Award/><h2>Subject progress</h2></div><div className="bars">{subjects.map(s=>{const ls=all.filter(l=>l.subject===s.id);const n=ls.filter(l=>completed.has(l.id)).length;const p=Math.round(n/Math.max(1,ls.length)*100);return <div className="barRow" key={s.id}><span>{s.icon} {s.name}</span><div><i style={{width:`${p}%`}}/></div><b>{p}%</b></div>})}</div></Card><div className="badgeGrid"><Card><Target/><h3>Daily goal</h3><b>{Math.min(child.minutes,child.dailyGoal)} / {child.dailyGoal} min</b><small>Complete a little every day.</small></Card><Card><CheckCircle2/><h3>First steps</h3><b>{completed.size>0?'Unlocked':'Locked'}</b><small>Finish your first learning activity.</small></Card></div></div>}
